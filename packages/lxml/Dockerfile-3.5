FROM imrehg/armv7hf-python:3.5

VOLUME /usr/src/target

RUN apt-get update && \
    apt-get install -yq --no-install-recommends \
      libxml2-dev \
      libxslt-dev

ENTRYPOINT ["pip", "wheel", "-vvv", "--wheel-dir=/usr/src/target"]
